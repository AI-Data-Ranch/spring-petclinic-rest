TEST EXPLANATION:

PreReqs:
	Have uv installed

Setup:
	Add the mcp-config.json in the .copilot directory to your global .copilot folder/ replace any mcp info that is already there

Prompting:
	Use the SERENA_PAGINATION_PROMPT.md
